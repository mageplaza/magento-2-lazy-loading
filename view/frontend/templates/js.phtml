<?php
/**
 * Mageplaza
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Mageplaza.com license that is
 * available through the world-wide-web at this URL:
 * https://www.mageplaza.com/LICENSE.txt
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category    Mageplaza
 * @package     Mageplaza_LazyLoading
 * @copyright   Copyright (c) Mageplaza (https://www.mageplaza.com/)
 * @license     https://www.mageplaza.com/LICENSE.txt
 */
?>
<script>
    require([
        'jquery',
        'lazyLoad',
        // 'Mageplaza_LazyLoading/js/lib/jquery.unveil'
    ], function ($) {
        $(document).ready(function() {
            $('.mplazyload').lazy({
                threshold: <?= $block->getThreshold() ?>,
                effect: "fadeIn",
                effectTime: 1000,
                afterLoad: function(e) {
                    e.removeClass('mplazyload-blur');
                    e.removeClass('mplazyload-icon');
                    e.removeClass('mplazyload-cms');
                },
            });
            // $('.mplazyload').trigger('lazy');
            //$('.mplazyload').unveil(<?//= $block->getThreshold() ?>//, function() {
            //    $(this).load(function() {
            //        // this.style.opacity = 1;
            //        $(this).removeClass('mplazyload-blur');
            //        $(this).removeClass('mplazyload-icon');
            //        $(this).removeClass('mplazyload-cms');
            //    });
            //});
        });
    });
</script>

<style>
    img.mplazyload-icon {
        /*background: url(*/<?//= $block->getIcon() ?>/*);*/
        width: <?= $block->getResizeWith() . 'px' ?>;
        height: <?= $block->getResizeHeight() . 'px' ?>;
    }

    img.mplazyload-cms {
        padding-left: 50%;
    }

    img.mplazyload-blur {
        transition: 0.3s filter linear;
        -o-transition: 0.3s -o-filter linear;
        -webkit-filter: blur(5px);
        filter: blur(5px);
    }
</style>
